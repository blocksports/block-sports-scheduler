# block-sports-scheduler

Fetches and stores data in a redis db container

## Installation
Build and run with

`docker-compose up`

Set environment variables in .env using:

`cat .env.example > .env`

